pipeline:
  name: hello-world-pipeline

  nodes:
    hello_world_node:
      class: my_hello_world.nodes.HelloWorldNode
      # This node does not have inputs or outputs as it's a simple hello world example.

  # Since this pipeline does not use any datasets, we don't need to define them here.
